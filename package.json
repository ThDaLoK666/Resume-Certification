{
  "name": "resume-certification",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "generate": "node scripts/generate-certs.mjs",
    "dev": "node scripts/generate-certs.mjs && vite",
    "build": "node scripts/generate-certs.mjs && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
